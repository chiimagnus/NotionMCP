[Unit]
Description=NotionMCP
After=network-online.target

[Service]
Type=simple
WorkingDirectory={{ROOT}}
ExecStart={{NODE}} {{CLI}} start
ExecStartPost=-{{NODE}} {{CLI}} doctor
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
